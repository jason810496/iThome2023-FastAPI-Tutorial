
# def add(x : int , y : int):
#     return x + y

def add(x, y):
    return x + y

# def merge(a : str , b : str):
#     return f"{a} with {b}"

def merge(a, b):
    return f"{a} with {b}"

# add()

merge()